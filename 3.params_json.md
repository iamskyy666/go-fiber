```go
// all-params
package main

import (
	"fmt"
	"log"

	"github.com/gofiber/fiber/v2"
)

func main() {
	app := fiber.New()
	app.Get("/user/:name/:age", func (ctx *fiber.Ctx)error  {
		params:=ctx.AllParams()
		fmt.Println("name:",params["name"])
		fmt.Println("age:",params["age"])

		return ctx.SendStatus(fiber.StatusOK)
	})


	fmt.Println("Server Up N Running.. ✅")
	log.Fatal(app.Listen(":3000"))
}

// O/P -
// name: Skyy
// age: 30
```

```go
package main

import (
	"fmt"
	"log"

	"github.com/gofiber/fiber/v2"
)

func main() {
	// download

	app := fiber.New()
	app.Get("/download", func (ctx *fiber.Ctx)error  {
		return ctx.Download("./files/sample.txt")
	})


	fmt.Println("Server Up N Running.. ✅")
	log.Fatal(app.Listen(":3000"))
}
```

```go
package main

import (
	"fmt"
	"log"

	"github.com/gofiber/fiber/v2"
)

type Pokemon struct{
	Name string `json:"pkemonName"`
	Trainer string `json:"trainerName"`
}

func main() {
	// bodyParser

	app := fiber.New()
	app.Post("/api/pokemon", func (ctx *fiber.Ctx)error  {
		p:=new(Pokemon)

		if err:=ctx.BodyParser(p); err!=nil{
			return err
		}

		// log.Println(p.Name)
		// log.Println(p.Trainer)

		return ctx.Status(fiber.StatusOK).JSON(fiber.Map{
			"name":p.Name,
			"trainer":p.Trainer,
		})
	})


	fmt.Println("Server Up N Running.. ✅")
	log.Fatal(app.Listen(":3000"))
}

// JSON-Body:
// {
//   "name":"Blaziken 🔥",
//   "trainer":"May"
// }
```
```json

```
